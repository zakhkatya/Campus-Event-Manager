databases:
  - name: ems_db
    plan: free
    databaseName: ems
    user: ems

services:
  - type: web
    plan: free
    name: ems
    runtime: python
    buildCommand: './build.sh'
    startCommand: 'cd ems && gunicorn ems.asgi:application -k uvicorn.workers.UvicornWorker'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ems_db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4